name: Feed — universe

on:
  schedule:
    - cron: '0 21 * * 0-4'   # 02:30 IST Mon-Fri
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - run: npx tsx scripts/feed-universe.ts

      - name: Commit slice
        run: |
          git config user.name "kosh-bot"
          git config user.email "kosh-bot@users.noreply.github.com"
          git pull --rebase --autostash
          git add data/feed
          git diff --staged --quiet || git commit -m "chore(feed): universe $(date -u +%F)"
          git push
